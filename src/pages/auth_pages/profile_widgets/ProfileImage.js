import React from 'react';

const ProfileImage = (data) => {
    const user_data = data.data;

    return (
        <div>
            *дефолтная картинка*
        </div>
    );
};

export default ProfileImage;