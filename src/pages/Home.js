import React from 'react';
import {observer} from "mobx-react-lite";

const Home = observer(() => {
    return (
        <>
            <div>
                Hello Home
            </div>
        </>
    );
});

export default Home;