import React from 'react';
import {observer} from "mobx-react-lite";


const Teams = observer(() => {
    return (
        <>
        <div>
            All Teams
        </div>
        </>
    );
});

export default Teams;